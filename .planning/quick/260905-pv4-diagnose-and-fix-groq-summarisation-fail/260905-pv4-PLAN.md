---
quick_id: 260905-pv4
phase: quick-260905-pv4-long-video-summarisation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - skimd-extension/src/shared/groq.js
  - skimd-extension/src/features/summary/summaryScheduler.js
  - skimd-extension/src/features/summary/useSummary.js
  - skimd-extension/src/shared/components/errorMessages.js
  - skimd-extension/test/groq-summary.test.js
  - skimd-extension/test/groq-reliability.test.js
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Captioned videos are summarized based on transcript size, not an unexplained video-duration cutoff near six minutes."
    - "The investigation directly determines whether the failure boundary is Skimd's abort/deadline policy, Groq token/context/rate limits, provider latency or availability, or another size-dependent path."
    - "Longer transcripts retain representative coverage from the beginning, middle, and end; the extension never silently drops a large suffix to make the request pass."
    - "HTTP failures, provider throttling, client aborts, and transport failures retain distinct truthful local codes and user messages."
    - "The prior key-validation, transcript-fetching, supported-model, serialized-request, bounded-retry, and cache-integrity behavior remains passing."
    - "No API key, Authorization header, transcript text, generated summary, or raw Groq response body is captured in tests, diagnostics, logs, screenshots, or commits."
  artifacts:
    - path: "skimd-extension/test/groq-reliability.test.js"
      provides: "Failing-first threshold, latency, request-count, and failure-class regressions"
    - path: "skimd-extension/src/shared/groq.js"
      provides: "Evidence-backed request sizing/deadline behavior and truthful provider classification"
    - path: "skimd-extension/src/features/summary/summaryScheduler.js"
      provides: "Evidence-backed multi-format scheduling or aggregation policy without repeated full-input amplification"
    - path: "skimd-extension/src/shared/components/errorMessages.js"
      provides: "Safe user guidance that distinguishes timeout, throttling, request rejection, and provider unavailability"
  key_links:
    - from: "skimd-extension/src/features/summary/summaryScheduler.js"
      to: "skimd-extension/src/shared/groq.js"
      via: "format scheduling and transcript-bearing completion calls"
      pattern: "summariseFormats|summarise"
    - from: "skimd-extension/src/shared/groq.js"
      to: "https://api.groq.com/openai/v1/chat/completions"
      via: "bounded authenticated request with safe status classification"
      pattern: "fetchWithRetry\\(GROQ_URL"
    - from: "skimd-extension/src/features/summary/useSummary.js"
      to: "chrome.storage.session"
      via: "save only a complete successful summary set"
      pattern: "saveSummaries"
---

<objective>
Diagnose and fix the transcript-size-dependent Groq summarisation failure reported on videos longer than roughly six minutes.

Purpose: Replace the misleading duration correlation with measured request evidence, identify whether Skimd or Groq owns the failure boundary, and make longer supported transcripts summarize reliably without hiding content loss or misclassifying errors.
Output: Privacy-safe causal evidence, a failing-first threshold regression, the smallest proven correction, truthful error handling, and runtime/browser verification across the failure boundary.
</objective>

<execution_context>
@/Users/j/.codex/gsd-core/workflows/execute-plan.md
@/Users/j/.codex/plugins/cache/sisyphuslabs/omo/0.1.0/skills/debugging/SKILL.md
@/Users/j/.codex/plugins/cache/sisyphuslabs/omo/0.1.0/skills/programming/SKILL.md
@/Users/j/.agents/skills/agent-browser/SKILL.md
</execution_context>

<context>
@AGENTS.md
@PRD.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/quick/260905-o14-diagnose-and-fix-the-summarise-action-fa/260905-o14-SUMMARY.md
@.planning/quick/260905-ovv-diagnose-and-fix-intermittent-groq-busy-/260905-ovv-SUMMARY.md
@skimd-extension/package.json
@skimd-extension/src/shared/groq.js
@skimd-extension/src/features/summary/summaryScheduler.js
@skimd-extension/src/features/summary/useSummary.js
@skimd-extension/src/features/summary/SummaryPanel.jsx
@skimd-extension/src/shared/components/errorMessages.js
@skimd-extension/test/groq-summary.test.js
@skimd-extension/test/groq-reliability.test.js
@skimd-extension/test/groq-settings.test.js
@skimd-extension/test/youtube-transcript.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Measure and causally identify the transcript-size failure boundary</name>
  <files>skimd-extension/test/groq-reliability.test.js, skimd-extension/test/groq-summary.test.js</files>
  <behavior>
    - A privacy-safe threshold sweep records transcript character count, conservative token estimate, format/request ordinal, HTTP status or local error code, retry count, and elapsed milliseconds without recording content.
    - Short, boundary, and above-boundary inputs distinguish an HTTP response from a client-side abort or transport exception.
    - One-variable toggles prove or exclude Skimd's 30-second attempt timeout, the 45-second total per-request budget, repeated full-transcript token consumption across three formats, Groq request/context rejection, token/rate limiting, and transient provider latency.
    - A deterministic regression fails for the same confirmed size-dependent mechanism before production code changes.
  </behavior>
  <action>Follow the debugging skill's required Node/browser and methodology references before running probes. Journal every temporary artifact in `.debug-journal.md`, exclude only that exact file locally, and snapshot `git status --short` before edits. Form at least four orthogonal hypotheses: H1 the current 30-second attempt abort or 45-second total deadline expires as input latency rises; H2 sending the complete transcript separately for Paragraph, Bullets, and TLDR exhausts a Groq token-per-minute or related account/model limit; H3 a single request crosses the model/context/request-size boundary and receives an HTTP 4xx response; H4 size correlates with a provider 5xx/latency or another client path such as serialization or parsing. Use synthetic inputs with no user content and sweep by character count plus a documented conservative token estimate, bracketing the observed boundary with at least two passing sizes and two failing sizes; do not use video minutes as the independent variable. For a sanitized live probe where the locally saved key is accessible, capture only input length/token estimate, ordinal, elapsed time, attempt count, HTTP status/local code, and allowlisted numeric rate-limit/reset metadata; never capture request bodies, response bodies, headers wholesale, credentials, transcript text, or generated text. Toggle one factor at a time and revert it: one request versus the existing three-format sequence at identical size, abort enabled versus a bounded larger diagnostic deadline, below/above-boundary size at identical format, and controlled HTTP 400/413/422/429/5xx versus an AbortError. An HTTP response proves Groq answered and must not be called connectivity failure; a locally fired AbortController proves an extension/client timeout; no conclusion may be based on code inspection alone. State the answer explicitly as extension timeout/retry logic, Groq token/context/rate behavior, provider latency/availability, another path, or multiple independently confirmed causes. Add red-first deterministic tests that reproduce the confirmed mechanism and observe the expected failure before any production edit. Preserve all current production behavior in this task.</action>
  <verify>
    <automated>npm --prefix skimd-extension test</automated>
  </verify>
  <done>The size boundary is expressed in characters and estimated tokens, request latency is measured, at least four hypotheses have distinguishing runtime evidence, causal toggle-and-revert proof directly answers whether Skimd or Groq owns the failure, and a regression fails for that same cause before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the smallest evidence-backed long-input correction</name>
  <files>skimd-extension/src/shared/groq.js, skimd-extension/src/features/summary/summaryScheduler.js, skimd-extension/src/features/summary/useSummary.js, skimd-extension/src/shared/components/errorMessages.js, skimd-extension/test/groq-summary.test.js, skimd-extension/test/groq-reliability.test.js</files>
  <behavior>
    - Passing inputs below and above the former boundary complete all three summary formats under a bounded, deterministic policy.
    - If client timeout is causal, the deadline scales or is restructured from measured latency while retaining a finite user-visible upper bound and correct AbortError classification.
    - If repeated input tokens or Groq rate limits are causal, the completed design avoids unnecessary full-transcript multiplication and obeys provider reset guidance without request amplification.
    - If input size is causal, any budgeting, chunking, compression, or hierarchical summarisation uses deterministic coverage from the transcript beginning, middle, and end and tests that representative markers survive; suffix-only truncation is forbidden.
    - HTTP 4xx/429/5xx, client abort, and network exceptions remain distinguishable, safe, and truthful; incomplete result sets are not cached.
  </behavior>
  <action>Implement only the causal correction proved in Task 1 and retain `openai/gpt-oss-120b` unless live provider evidence proves the model contract itself changed. Use this decision table: for a local timeout, adjust the attempt/operation budget or request shape using measured p95-style headroom and keep an explicit finite cap; for repeated-token or 429 pressure, reduce redundant transcript transmission through an evidence-backed single structured completion or another bounded scheduler policy, validating exact call/input counts and all three outputs; for context/request-size rejection, introduce a deterministic token-budget/chunk-and-reduce policy whose tests prove representative beginning/middle/end coverage and whose prompts preserve the requested formats; for transient 5xx/provider latency, refine only the bounded retry/deadline interaction. Do not merely lengthen every timeout, retry 4xx responses, relabel a provider response as connectivity trouble, or discard a large suffix. Parse and expose only stable local codes; do not surface or persist raw provider bodies or unrestricted headers. Keep validation on `GET /models`, transcript retrieval, supported-model fields, bounded retry counts/delays, serialized/no-amplification guarantees unless the proven replacement is safer, tab/video cache scoping, and all-or-nothing summary caching. Make the red tests green, add exact request-count and elapsed-budget assertions with injected deterministic timing where needed, and add boundary cases for below/at/above the old failure size plus each relevant terminal class.</action>
  <verify>
    <automated>npm --prefix skimd-extension test && npm --prefix skimd-extension run build</automated>
  </verify>
  <done>The confirmed cause is minimally fixed; boundary-sized and larger synthetic inputs complete under deterministic tests; representative transcript coverage is proven if transformation is used; HTTP, abort, throttling, and transport errors remain truthful; and all existing key, transcript, model, scheduling, cache, and build checks pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify short and long production flows and remove all diagnostics</name>
  <files>.debug-journal.md</files>
  <action>Build and reload the unpacked production extension, then use a real browser to exercise Summarise and Regenerate on at least one short captioned video and two longer captioned videos whose transcript character counts bracket the former boundary. Confirm Paragraph, Bullets, and TLDR render, the button returns from its loading state, complete results cache together, and no blocking popup/service-worker errors occur. Record only video duration for orientation, transcript character/cue count, conservative token estimate, request ordinal/count, sanitized outcome code/status class, elapsed time, and output length; duration must not be presented as the technical threshold. Where safely controllable, exercise a client abort and an HTTP 429 or 5xx to verify their distinct final messages. If the automation browser cannot load the extension, access the saved key, or run live Groq calls, state the exact limitation and label browser/provider claims partial; substitute deterministic built-popup and direct request-boundary evidence without claiming end-to-end success. Walk the debug journal and remove every temporary fixture, interceptor, process, trace, screenshot, probe, and instrumentation edit. Remove `.debug-journal.md` and only its exact `.git/info/exclude` entry. Compare final status with the starting snapshot, preserve unrelated dirty files, scan scoped changes for secrets/user/provider content and debug output, then rerun the full tests and production build after cleanup.</action>
  <verify>
    <automated>test ! -e .debug-journal.md && ! git diff -- skimd-extension/src/shared/groq.js skimd-extension/src/features/summary/summaryScheduler.js skimd-extension/src/features/summary/useSummary.js skimd-extension/src/shared/components/errorMessages.js skimd-extension/test/groq-summary.test.js skimd-extension/test/groq-reliability.test.js | rg -n "Authorization: Bearer [^$]|gsk_[A-Za-z0-9]|TRANSCRIPT:\\n[^$]|console\\.(log|debug)\\(|debugger;|\\[DEBUG\\]" && npm --prefix skimd-extension test && npm --prefix skimd-extension run build</automated>
    <human-check>After reloading Skimd, summarize a captioned video below the former boundary and two above it; confirm all three formats render, loading settles, Regenerate works, and any genuine provider or timeout failure shows one accurate message. Label substitute evidence partial if the installed extension or saved key cannot be accessed.</human-check>
  </verify>
  <done>Runtime/browser evidence covers both sides of the measured boundary or is honestly labeled partial, the extension no longer exhibits the confirmed size-dependent defect, error states remain distinct, no sensitive or temporary artifact remains, unrelated worktree changes are untouched, and the final suite/build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup to Groq API | A local credential and untrusted transcript cross into a third-party API request. |
| Groq response to retry/deadline logic | Untrusted statuses, rate metadata, timing, and JSON control retries, errors, and completion. |
| Transcript transformation to summary request | Any size-reduction policy can silently omit user-visible content if coverage is not explicit and tested. |
| Runtime investigation to local artifacts | Diagnostics can leak secrets, watched-video content, summaries, or provider payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260905-pv4-01 | Information Disclosure | Groq probes and debug journal | high | mitigate | Record only counts, estimates, elapsed time, status/local codes, and allowlisted numeric limit metadata; prohibit keys, headers wholesale, bodies, transcript text, summaries, and raw provider responses; scrub all artifacts. |
| T-260905-pv4-02 | Denial of Service | Multi-format full-transcript requests | high | mitigate | Measure exact token/input multiplication, request count, latency, and rate behavior; implement a bounded evidence-backed policy without retry amplification. |
| T-260905-pv4-03 | Tampering | Provider status and rate/reset metadata | medium | mitigate | Treat all provider data as untrusted, clamp any reset delay, parse only allowlisted fields, and map to stable local codes. |
| T-260905-pv4-04 | Information Disclosure | Transcript budgeting or chunking | high | mitigate | Use synthetic marker tests to prove beginning/middle/end representative coverage and forbid silent suffix truncation. |
| T-260905-pv4-05 | Repudiation | Timeout versus provider-failure diagnosis | medium | mitigate | Preserve a sanitized causal timeline and require toggle-and-revert proof distinguishing HTTP responses, local aborts, and transport exceptions. |
</threat_model>

<verification>
1. A privacy-safe threshold sweep measures transcript characters, estimated tokens, request ordinal/count, and latency across at least two passing and two failing sizes.
2. Causal toggles explicitly determine whether the boundary comes from the current 30-second attempt/45-second request deadline, repeated transcript-token consumption, Groq context/request limits, token/rate limits, provider latency, or another size-dependent path.
3. A deterministic failing-first regression reproduces the confirmed mechanism before production edits and passes after the smallest correction.
4. If content transformation is introduced, synthetic boundary markers prove representative beginning, middle, and end coverage; no suffix-only truncation is accepted.
5. HTTP responses, 429 throttling, client aborts, and transport exceptions retain distinct stable classifications and messages.
6. Full tests and production build pass, including previous key-validation, transcript-fetching, model-contract, retry, scheduler, and cache behavior.
7. Real short/long extension QA is performed where possible; any blocked live-provider or browser evidence is explicitly labeled partial with the blocker.
8. Final cleanup proves no credential, Authorization value, transcript/summary content, provider body, diagnostic artifact, or unrelated worktree mutation remains.
</verification>

<success_criteria>
- The user receives a direct evidence-backed answer about whether Skimd's timeout/retry logic, Groq limits/latency, or another path caused the apparent six-minute boundary.
- Supported longer transcripts summarize reliably without unexplained duration cutoffs, redundant request amplification, or silent loss of transcript coverage.
- Genuine provider rejection, throttling, unavailability, client timeout, and network failure produce truthful distinct guidance.
- Paragraph, Bullets, and TLDR remain complete and cache only as a successful set.
- Existing key saving, transcript retrieval, model selection, retry bounding, and unrelated user changes remain intact.
</success_criteria>

<source_audit>
| Source | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | - | Diagnose and fix Groq failures correlated with videos over about six minutes | 01 | COVERED | Tasks 1-3 measure the real size boundary, implement the proven fix, and verify the production flow. |
| REQ | - | No ROADMAP phase requirement IDs are assigned to this quick task | - | COVERED | Quick work is tracked outside phase requirements. |
| RESEARCH | - | No separate research artifact was requested | - | COVERED | Task 1 performs focused live/runtime discovery before selecting a correction. |
| CONTEXT | - | Answer extension timeout versus Groq token/context/rate behavior versus another path | 01 | COVERED | Task 1 mandates orthogonal hypotheses, threshold sweeps, and causal toggles. |
| CONTEXT | - | Measure transcript characters/token estimate and latency rather than video minutes | 01 | COVERED | Tasks 1 and 3 use size metrics and bracket the observed boundary. |
| CONTEXT | - | Preserve truthful HTTP versus abort/transport errors | 01 | COVERED | Tasks 1-2 explicitly test and maintain distinct classifications. |
| CONTEXT | - | Never capture secrets, content, or raw provider bodies | 01 | COVERED | All tasks restrict evidence and Task 3 scrubs and scans artifacts. |
| CONTEXT | - | Avoid silent transcript loss; preserve representative coverage | 01 | COVERED | Task 2 requires beginning/middle/end marker coverage for any transformation. |
| CONTEXT | - | Preserve prior fixes and unrelated dirty files | 01 | COVERED | Tasks 2-3 lock regressions and compare worktree snapshots. |
</source_audit>

<output>
Create `.planning/quick/260905-pv4-diagnose-and-fix-groq-summarisation-fail/260905-pv4-SUMMARY.md` when done.
</output>
