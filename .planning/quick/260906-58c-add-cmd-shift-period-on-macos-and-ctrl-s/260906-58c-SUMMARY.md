---
quick_id: 260906-58c
phase: quick-260906-58c-keyboard-shortcut
plan: 01
subsystem: extension-action
tags: [chrome-extension, keyboard-shortcut, manifest-v3, popup]
requires:
  - quick-260905-qph: readiness-gated automatic summarisation on popup open
provides:
  - Native Chrome action shortcut for opening Skimd
  - Platform-specific Command and Control bindings
affects: [manifest, popup-entry, service-worker]
tech-stack:
  added: []
  patterns: [reserved execute-action command, shared toolbar-and-shortcut popup entry]
key-files:
  created:
    - skimd-extension/test/keyboard-shortcut.test.js
  modified:
    - skimd-extension/public/manifest.json
    - skimd-extension/src/background/service-worker.js
    - skimd-extension/src/popup/App.jsx
    - skimd-extension/src/features/summary/SummaryPanel.jsx
key-decisions:
  - "Use Chrome's reserved _execute_action command so shortcut and toolbar activation open the same configured popup."
  - "Let Chrome own shortcut conflict resolution and expose user overrides through the browser shortcuts page."
requirements-completed: []
coverage:
  - deliverable: Exact platform shortcuts open the configured extension action
    verification:
      - kind: test
        ref: skimd-extension/test/keyboard-shortcut.test.js
        status: pass
      - kind: browser
        ref: Edge keyboard shortcuts page effective assignment
        status: pass
    human_judgment: false
  - deliverable: Shortcut and toolbar share the existing popup and automatic-summary path
    verification:
      - kind: test
        ref: skimd-extension/test/auto-summarise.test.js
        status: pass
      - kind: browser
        ref: packaged popup load in clean extension profile
        status: pass
    human_judgment: false
duration: 7min
completed: 2026-09-06
status: complete
actuals:
  tokens: 2083
  tasks: 2
  commits: 2
---

# Quick Task 260906-58c: Native Keyboard Shortcut Summary

**Skimd now opens through Chrome's native extension action using Command+Shift+Period on macOS and Ctrl+Shift+Period on Windows and Linux.**

## Accomplishments

- Replaced the custom `summarise` command with the reserved `_execute_action` command while retaining `popup.html` as the action target.
- Removed the redundant service-worker `openPopup()` call, session-storage signal, popup listener, and keyboard-only summary effect.
- Preserved the existing readiness gate, per-video popup claim, cache restoration, manual controls, and request single-flight behavior.
- Added failing-first regression coverage for the source manifest, exact platform bindings, popup target, and absence of the obsolete bridge.

## Task Commits

1. **Task 1 RED: Add failing native-shortcut assertions** — `efe64d2`
2. **Task 1 GREEN: Route the shortcut through Chrome's native action** — `4fc99fa`

Task 2 verified the production build and real browser registration and required no source commit.

## Verification

- The focused RED run failed against the prior `summarise` command and custom storage/open-popup bridge.
- Full suite passed: 31 tests, 0 failures.
- Production build passed with 44 modules transformed.
- The built manifest contains `_execute_action`, `Ctrl+Shift+Period`, `Command+Shift+Period`, and `action.default_popup: popup.html` with no legacy `summarise` command.
- Microsoft Edge loaded the unpacked production extension in a clean profile and displayed Skimd's effective action shortcut as `⇧⌘.` with no conflict or unassigned state.
- The packaged popup loaded from the registered extension origin without page errors and entered the existing missing-key guard in the clean profile.
- No API key, authorization header, transcript, provider payload, or summary content was captured during browser QA.

## Browser QA Limitation

CDP-generated key events do not exercise the browser's OS-level extension accelerator, so automated QA could inspect the effective assigned command but could not conclusively simulate the physical shortcut. The native `_execute_action` mapping and shared `popup.html` target are deterministic; the user should confirm the physical shortcut once after reloading the extension.

## Deviations from Plan

- Real-browser inspection used Microsoft Edge's Chromium extension runtime because the automation-managed Chrome launch did not expose unpacked extensions. Edge registered the requested macOS shortcut without a conflict.
- The repository's existing ESLint configuration still reports undeclared Chrome globals in the service worker. The scoped React and test files introduced no new lint failure category.
- The programming skill's optional no-excuse audit script could not run because its own `typescript` package dependency is unavailable; the task changes were manually inspected and the full project test/build gates passed.

## Known Stubs

None.

## Self-Check: PASSED

- All five planned source and test files exist; the production manifest was generated and parsed successfully.
- Commits `efe64d2` and `4fc99fa` exist.
- Tests, production build, built-manifest assertions, effective browser assignment, packaged popup load, privacy checks, and temporary-profile cleanup passed.
- Unrelated working-tree files were not modified.
