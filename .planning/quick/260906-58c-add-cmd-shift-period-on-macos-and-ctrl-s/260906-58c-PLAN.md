---
quick_id: 260906-58c
phase: quick-260906-58c-keyboard-shortcut
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - skimd-extension/public/manifest.json
  - skimd-extension/src/background/service-worker.js
  - skimd-extension/src/popup/App.jsx
  - skimd-extension/src/features/summary/SummaryPanel.jsx
  - skimd-extension/test/keyboard-shortcut.test.js
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Pressing Command+Shift+Period on macOS or Ctrl+Shift+Period on Windows/Linux invokes the same Skimd popup action as clicking the toolbar icon."
    - "Opening the popup by shortcut enters the existing readiness-gated automatic summarisation flow without requiring a second click."
    - "Toolbar clicks, cached-summary restoration, missing-key handling, transcript guards, and duplicate-request prevention remain unchanged."
    - "The shortcut declaration is present in the production manifest; Chrome may leave a suggested shortcut unassigned when it conflicts with another command, and users can inspect or override it at chrome://extensions/shortcuts."
  artifacts:
    - path: "skimd-extension/public/manifest.json"
      provides: "Chrome `_execute_action` command with platform-specific Period shortcuts"
    - path: "skimd-extension/test/keyboard-shortcut.test.js"
      provides: "Deterministic source-manifest and obsolete-bridge regression assertions"
    - path: "skimd-extension/dist/manifest.json"
      provides: "Production-built shortcut declaration consumed by Chrome"
  key_links:
    - from: "skimd-extension/public/manifest.json"
      to: "skimd-extension/action.default_popup"
      via: "reserved `_execute_action` command"
      pattern: "_execute_action|Command\\+Shift\\+Period|Ctrl\\+Shift\\+Period"
    - from: "Chrome action dispatch"
      to: "skimd-extension/src/popup/App.jsx"
      via: "opening popup.html through the existing action"
      pattern: "default_popup.*popup.html"
    - from: "skimd-extension/src/popup/App.jsx"
      to: "skimd-extension/src/features/summary/SummaryPanel.jsx"
      via: "existing popup-lifetime claim callback and readiness policy"
      pattern: "claimAutoSummarise|isAutoSummariseReady"
---

<objective>
Make the selected cross-platform keyboard shortcut open Skimd exactly like a toolbar click and naturally trigger the existing automatic summarisation flow.

Purpose: Let users start Skimd without reaching for the toolbar while retaining the popup's established cache, readiness, and single-flight safeguards.
Output: A native Chrome action shortcut, removal of the redundant custom storage/openPopup bridge, focused manifest regression coverage, and production Chrome QA.
</objective>

<execution_context>
@/Users/j/.codex/gsd-core/workflows/execute-plan.md
@/Users/j/.codex/plugins/cache/sisyphuslabs/omo/0.1.0/skills/programming/SKILL.md
@/Users/j/.agents/skills/agent-browser/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/quick/260905-qph-automatically-start-summarising-the-curr/260905-qph-SUMMARY.md
@skimd-extension/package.json
@skimd-extension/public/manifest.json
@skimd-extension/vite.config.js
@skimd-extension/src/background/service-worker.js
@skimd-extension/src/popup/App.jsx
@skimd-extension/src/features/summary/SummaryPanel.jsx
@skimd-extension/src/features/summary/autoSummarise.js
@skimd-extension/test/auto-summarise.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route the platform shortcut through Chrome's native popup action</name>
  <files>skimd-extension/public/manifest.json, skimd-extension/src/background/service-worker.js, skimd-extension/src/popup/App.jsx, skimd-extension/src/features/summary/SummaryPanel.jsx, skimd-extension/test/keyboard-shortcut.test.js</files>
  <behavior>
    - The manifest exposes exactly one reserved `_execute_action` command whose default key is `Ctrl+Shift+Period` and macOS key is `Command+Shift+Period`.
    - The action continues to target `popup.html`, so keyboard invocation and toolbar invocation share the same popup entry point.
    - The old named `summarise` command, `chrome.commands.onCommand` listener, `chrome.action.openPopup()` call, and `autoSummarise` session-storage trigger are absent.
    - Popup opening still uses the existing per-video readiness claim and request single-flight path; neither keyboard nor toolbar opening creates an extra summary request.
  </behavior>
  <action>First add `test/keyboard-shortcut.test.js` with Node's existing test runner. Read and parse `public/manifest.json` and assert the reserved `_execute_action` command, exact default/mac key strings selected by the user, action popup target, and absence of the old custom command. Add deterministic source assertions that the service worker and popup no longer contain the custom command/session flag/openPopup bridge; observe the test fail against the current `summarise` command before production edits. Then replace the custom manifest command with `_execute_action`. Remove only the obsolete `chrome.commands.onCommand` handler from `service-worker.js`, the keyboard-only `pendingSummarise` state/storage listener from `App.jsx`, its trigger props at the SummaryPanel call site, and the corresponding keyboard-trigger effect/props from `SummaryPanel.jsx`. Do not alter the existing generic automatic-summary effect, popup-lifetime claim ref, `summariseAll` single-flight behavior, manual Summarise/Regenerate controls, or any Groq/transcript logic. The reserved command must let Chrome open `action.default_popup` directly, making shortcut activation equivalent to a toolbar click without relying on `chrome.action.openPopup()` browser/version support.</action>
  <verify>
    <automated>npm --prefix skimd-extension test</automated>
  </verify>
  <done>The failing manifest regression turns green; source contains one native action command with the requested platform keys; the redundant custom bridge is gone; and all existing automatic-summary and integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the built shortcut and real popup behavior</name>
  <files>skimd-extension/dist/manifest.json</files>
  <action>Run the production build and parse `dist/manifest.json` to prove Vite copied the command unchanged: `_execute_action` targets `Ctrl+Shift+Period` by default and `Command+Shift+Period` on macOS, while `action.default_popup` remains `popup.html`. Reload the unpacked extension in Chrome, inspect `chrome://extensions/shortcuts`, and confirm Skimd exposes the action command with the expected platform binding when no browser/extension conflict owns it. On a captioned YouTube watch page with the already-saved valid key, invoke the shortcut and confirm the popup opens, automatically progresses through the established summarisation flow, and produces no duplicate request; also click the toolbar icon once to confirm unchanged behavior and reopen a cached result to confirm no provider request. If Chrome reports the shortcut unassigned because of a local conflict, record that exact browser state and verify assigning the requested combination manually works; do not claim Chrome can forcibly override another extension or browser shortcut. Do not capture the API key, Authorization header, transcript, provider request/response body, or generated summary content. Remove temporary browser traces or harness files and preserve unrelated worktree changes.</action>
  <verify>
    <automated>npm --prefix skimd-extension run build &amp;&amp; node -e "const fs=require('fs');const m=JSON.parse(fs.readFileSync('skimd-extension/dist/manifest.json','utf8'));const c=m.commands&amp;&amp;m.commands._execute_action;if(!c||c.suggested_key.default!=='Ctrl+Shift+Period'||c.suggested_key.mac!=='Command+Shift+Period'||m.action.default_popup!=='popup.html'||m.commands.summarise)process.exit(1)" &amp;&amp; npm --prefix skimd-extension test</automated>
    <human-check>After reloading Skimd, open chrome://extensions/shortcuts and confirm the requested shortcut is assigned or assign it if Chrome reports a conflict. On a captioned YouTube video, press the shortcut and confirm Skimd opens and starts summarising exactly as it does from a toolbar click.</human-check>
  </verify>
  <done>The production manifest contains the exact cross-platform native action shortcut, feasible real-Chrome inspection confirms the command, shortcut and toolbar openings share the working auto-summary flow without duplicates, and any local shortcut conflict is reported truthfully with its Chrome override path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS/browser keyboard dispatch to extension action | Chrome owns shortcut registration and may decline a suggested key when another browser or extension command conflicts. |
| Chrome action dispatch to popup lifecycle | Keyboard activation must open the same packaged popup as a toolbar click without an auxiliary storage signal. |
| Popup readiness to Groq request | The existing automatic-summary guard controls whether opening the popup creates a provider request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260906-58c-01 | Denial of Service | Shortcut-to-summary flow | medium | mitigate | Route through `_execute_action` and retain existing per-video claim plus request single-flight tests so one shortcut press cannot amplify provider calls. |
| T-260906-58c-02 | Tampering | Packaged manifest | medium | mitigate | Parse both source and production manifests and assert the exact command ID, platform keys, and popup target. |
| T-260906-58c-03 | Spoofing | Local Chrome shortcut assignment | low | accept | Chrome remains authoritative for conflicts and user overrides; inspect the effective command at chrome://extensions/shortcuts rather than implying forced ownership. |
| T-260906-58c-04 | Information Disclosure | Browser QA | high | mitigate | Observe command assignment, popup state, and request counts only; prohibit credentials and video/provider content from logs and artifacts. |
</threat_model>

<verification>
1. A failing-first deterministic test distinguishes the old custom `summarise` bridge from the required `_execute_action` declaration.
2. The complete Node suite proves existing auto-start readiness, cache suppression, duplicate protection, transcript behavior, and Groq behavior remain intact.
3. Production build succeeds and `dist/manifest.json` contains the exact platform keys plus `popup.html` action target.
4. Chrome command inspection verifies the effective assignment or truthfully identifies a local conflict and its user-controlled override path.
5. Shortcut opening, toolbar opening, automatic summary start, and cached reopen are exercised without capturing secrets or content payloads.
</verification>

<success_criteria>
- Command+Shift+Period on macOS and Ctrl+Shift+Period on Windows/Linux can open Skimd through Chrome's native action command.
- Shortcut activation behaves like a toolbar click and naturally reaches the existing one-request automatic-summary path.
- The obsolete custom command bridge is removed without changing toolbar, settings, transcript, cache, retry, or manual controls.
- Tests, production build, built-manifest assertions, and feasible Chrome QA pass; shortcut conflicts are accurately surfaced rather than hidden.
</success_criteria>

<source_audit>
| Source | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | - | Open/start Skimd from the selected keyboard shortcut as if the toolbar icon was clicked | 01 | COVERED | Task 1 uses the reserved action command; Task 2 exercises the popup path. |
| REQ | - | No ROADMAP phase requirement IDs are assigned to this quick task | - | COVERED | Quick work is tracked outside phase requirements. |
| RESEARCH | - | Chrome native action shortcut should replace the custom storage/openPopup bridge | 01 | COVERED | `_execute_action` directly invokes the configured popup action. |
| CONTEXT | - | Use Command+Shift+Period on macOS | 01 | COVERED | Exact manifest value and assertions are specified. |
| CONTEXT | - | Use Ctrl+Shift+Period on Windows-compatible platforms | 01 | COVERED | Chrome's `default` binding supplies Windows/Linux while macOS is overridden explicitly. |
| CONTEXT | - | Shortcut behaves like clicking Skimd and enters existing auto-summary flow | 01 | COVERED | Native action shares `popup.html`; old separate trigger path is removed. |
| CONTEXT | - | Preserve toolbar behavior and all automatic-summary safeguards | 01 | COVERED | Existing readiness, claim, cache, and single-flight logic is explicitly protected and regression-tested. |
| CONTEXT | - | Validate source manifest, production build, and real Chrome assignment where feasible | 01 | COVERED | Both tasks define deterministic assertions and browser inspection. |
| CONTEXT | - | Account for shortcut conflicts and overrides | 01 | COVERED | Task 2 checks effective assignment and documents Chrome's user-controlled override path only when needed. |
</source_audit>

<output>
Create `.planning/quick/260906-58c-add-cmd-shift-period-on-macos-and-ctrl-s/260906-58c-SUMMARY.md` when done.
</output>
