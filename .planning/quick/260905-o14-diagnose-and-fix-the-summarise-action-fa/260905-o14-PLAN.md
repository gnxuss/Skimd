---
quick_id: 260905-o14
phase: quick-260905-o14-summarise-action-failure
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - skimd-extension/src/shared/groq.js
  - skimd-extension/src/features/summary/useSummary.js
  - skimd-extension/src/features/summary/SummaryPanel.jsx
  - skimd-extension/src/shared/components/errorMessages.js
  - skimd-extension/test/groq-summary.test.js
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "With a saved valid Groq key and a non-empty transcript, clicking Summarise returns usable summary content through the production request path."
    - "Authentication, model/request rejection, rate limiting, provider outage, timeout, and transport failure remain distinguishable enough to show truthful user guidance instead of every failure becoming a connectivity claim."
    - "The working credential-validation flow and transcript retrieval flow remain unchanged and passing."
    - "Diagnosis and QA never record an API key, Authorization value, transcript content, or generated summary content."
  artifacts:
    - path: "skimd-extension/src/shared/groq.js"
      provides: "Groq summary request, sanitized response classification, and coded errors"
    - path: "skimd-extension/test/groq-summary.test.js"
      provides: "Failing-first coverage for the confirmed completion-request failure and error classes"
    - path: "skimd-extension/src/shared/components/errorMessages.js"
      provides: "Specific user-safe guidance for stable summary error codes"
  key_links:
    - from: "skimd-extension/src/features/summary/useSummary.js"
      to: "skimd-extension/src/shared/groq.js"
      via: "summarise calls for paragraph, bullets, and TLDR"
      pattern: "summarise\\(\\{ transcript, format, apiKey"
    - from: "skimd-extension/src/shared/groq.js"
      to: "https://api.groq.com/openai/v1/chat/completions"
      via: "authenticated POST with the configured model and completion payload"
      pattern: "GROQ_URL"
    - from: "skimd-extension/src/features/summary/SummaryPanel.jsx"
      to: "skimd-extension/src/shared/components/errorMessages.js"
      via: "coded or explicitly sanitized summary error rendered by ErrorBanner"
      pattern: "ErrorBanner"
---

<objective>
Diagnose and fix the Summarise action so a valid saved key and fetched transcript produce summaries, while failures display specific and truthful guidance.

Purpose: The core user flow currently reaches the Groq completion boundary but reports every observed failure as a connectivity problem, obscuring the real cause and preventing summaries.
Output: A causally proven request-path fix, failing-first regression coverage, sanitized provider-status classification, realistic browser evidence, and complete cleanup of diagnostics.
</objective>

<execution_context>
@/Users/j/.codex/gsd-core/workflows/execute-plan.md
@/Users/j/.codex/plugins/cache/sisyphuslabs/omo/0.1.0/skills/debugging/SKILL.md
@/Users/j/.codex/plugins/cache/sisyphuslabs/omo/0.1.0/skills/programming/SKILL.md
@/Users/j/.agents/skills/agent-browser/SKILL.md
</execution_context>

<context>
@AGENTS.md
@PRD.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/quick/260905-mtz-diagnose-and-fix-groq-api-key-validation/260905-mtz-SUMMARY.md
@.planning/quick/260905-njj-fix-youtube-transcript-fetching-so-suppo/260905-njj-SUMMARY.md
@skimd-extension/package.json
@skimd-extension/public/manifest.json
@skimd-extension/src/shared/groq.js
@skimd-extension/src/features/summary/useSummary.js
@skimd-extension/src/features/summary/SummaryPanel.jsx
@skimd-extension/src/shared/components/ErrorBanner.jsx
@skimd-extension/src/shared/components/errorMessages.js
@skimd-extension/test/groq-settings.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the completion failure and restore one end-to-end summary path</name>
  <files>skimd-extension/src/shared/groq.js, skimd-extension/test/groq-summary.test.js</files>
  <behavior>
    - A valid completion response returns trimmed summary text through `summarise({ transcript, format, apiKey })`.
    - The confirmed provider failure is classified from its HTTP status and sanitized provider error fields without retaining the request credential, transcript, or response content.
    - Credential rejection remains `INVALID_KEY`; transport and timeout failures remain availability errors; non-authentication HTTP failures are not automatically described as lost internet connectivity.
  </behavior>
  <action>Follow the debugging skill's hypothesis-driven loop before editing production code. Create a project-root `.debug-journal.md`, add only that exact path to the local git exclude, capture the starting `git status --short`, and ledger every temporary probe, browser process, trace, fixture, screenshot, instrumentation edit, and cleanup action. Form and test at least three distinct falsifiable hypotheses at the real `summarise` request boundary: H1 the hard-coded completion model is unavailable, retired, or unauthorized even though `GET /models` validates the credential; H2 the chat-completions payload or limits are rejected for the selected model; H3 three simultaneous format requests trigger throttling, quota, or another provider-side limit; H4 host permission, CORS, timeout, or fetch transport prevents a response. Capture only method, endpoint pathname, response status, provider error type/code/parameter after strict sanitization, attempt count, elapsed duration, and output length. Never capture the API key, Authorization header, request body, transcript text, response body, or generated summary text. Reproduce the failure twice, test every hypothesis, and require a causal toggle: changing only the suspected model/request/concurrency behavior must switch failure to success, while reverting it restores the same sanitized status/error class. Create `test/groq-summary.test.js` with Node's built-in test runner and encode the confirmed failure as a regression that is observed red before production edits. Implement the smallest correction in `groq.js`, preserving `validateKey` and the credential-only `/models` request exactly unless evidence proves a shared helper defect. Do not add a dependency or change transcript retrieval.</action>
  <verify>
    <automated>npm --prefix skimd-extension test</automated>
  </verify>
  <done>The journal contains two sanitized reproductions, evidence for or against at least three hypotheses, a causal toggle proving one root cause, the regression is observed red then green, and the production `summarise` boundary returns non-empty text for one supported format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve truthful summary errors and verify all three formats</name>
  <files>skimd-extension/src/shared/groq.js, skimd-extension/src/features/summary/useSummary.js, skimd-extension/src/features/summary/SummaryPanel.jsx, skimd-extension/src/shared/components/errorMessages.js, skimd-extension/test/groq-summary.test.js</files>
  <behavior>
    - Paragraph, bullets, and TLDR requests complete successfully under the confirmed provider contract and are cached only after the full requested result set succeeds.
    - HTTP 401/403, 400-class request/model failures, 429 throttling, 500-class provider failures, timeout, malformed JSON, and empty success bodies map to stable sanitized error codes or safe explicit messages.
    - The summary panel renders the mapped summary failure rather than discarding the hook's safe message and always falling back to the connectivity banner.
    - The earlier API-key validation tests and YouTube transcript tests remain passing.
  </behavior>
  <action>Extend the failing-first regression suite across the confirmed provider contract and the three-format orchestration. Keep error objects coded and safe at the `groq.js` boundary: parse only documented provider error metadata needed for classification, discard body text afterward, and never expose arbitrary provider payloads to the UI. Add or reuse narrowly named error codes/messages so authentication rejection, invalid model/request, throttling/quota, provider outage, timeout, transport failure, malformed response, and empty response receive truthful guidance. Preserve retry bounds and respect `Retry-After`; do not convert a provider HTTP response into a network claim. Update `useSummary.js`, `SummaryPanel.jsx`, or `errorMessages.js` only where live evidence and the regression show that safe classification is being lost between the service error and `ErrorBanner`. Preserve the successful validate-before-persist key flow, the transcript button guard, tab/video cache scope, and all three existing output formats. Run focused and full tests and the production build. Then use the built unpacked extension in Chromium on a public captioned YouTube video with the user's already-saved key: click `#btn-summarise`, wait for completion, switch among Paragraph, Bullets, and TLDR, and verify each has non-empty rendered output with no blocking console errors. Exercise at least one intercepted/synthetic non-secret provider error to verify its specific banner. If this environment still cannot expose unpacked extension pages or the user's locally stored key, document the exact blocker and combine a sanitized real provider probe at the production request boundary with deterministic built-popup/browser state, clearly labeling that evidence partial rather than claiming full live-extension success.</action>
  <verify>
    <automated>npm --prefix skimd-extension test && npm --prefix skimd-extension run build</automated>
    <human-check>In the unpacked production extension on a captioned YouTube video, click Summarise and confirm non-empty Paragraph, Bullets, and TLDR output plus no blocking console error; then confirm a controlled provider failure renders its specific safe guidance. If extension/key access is unavailable, record the exact limitation and the closest sanitized partial-runtime evidence.</human-check>
  </verify>
  <done>All three summary formats work under the confirmed provider contract, the principal provider and transport failure classes render truthful safe feedback, the full test suite and production build pass, and real browser QA or explicitly labeled partial evidence covers the original click path.</done>
</task>

<task type="auto">
  <name>Task 3: Remove diagnostics and prove the prior fixes remain intact</name>
  <files>.debug-journal.md</files>
  <action>Use the journal artifact ledger to stop and remove every temporary browser process, route interception, probe, fixture, trace, screenshot, instrumentation change, and generated output. Remove `.debug-journal.md` and only its exact `.git/info/exclude` entry. Compare final `git status --short` and scoped diffs with the recorded starting snapshot, preserving all unrelated user modifications and untracked files; never use broad reset, checkout, restore, or clean operations. Scan the final scoped diff for API-key patterns, Authorization values, transcript/summary payloads, provider response bodies, debug statements, logs, traces, and temporary markers. Retain only the causal summary fix, safe error classification/wiring, and regression coverage. Re-run the full suite and build after cleanup, explicitly confirming the Groq settings tests and YouTube transcript tests still pass.</action>
  <verify>
    <automated>test ! -e .debug-journal.md && ! git diff -- skimd-extension/src/shared/groq.js skimd-extension/src/features/summary/useSummary.js skimd-extension/src/features/summary/SummaryPanel.jsx skimd-extension/src/shared/components/errorMessages.js skimd-extension/test/groq-summary.test.js | rg -n "Authorization: Bearer [^$]|gsk_[A-Za-z0-9]|TRANSCRIPT:\\n[^$]|debugger;|\\[DEBUG\\]" && npm --prefix skimd-extension test && npm --prefix skimd-extension run build</automated>
  </verify>
  <done>No diagnostic or sensitive artifact remains, unrelated worktree changes match the starting snapshot, and summary, key-validation, transcript, and build checks all pass after cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup to Groq API | A locally stored user credential and untrusted transcript cross from the extension to an external provider over HTTPS. |
| Groq response to extension state | External status codes, JSON, rate-limit headers, and generated content enter service and React state. |
| Runtime diagnostics to local artifacts | Debug evidence can expose credentials, video content, generated content, or provider payloads unless strictly minimized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260905-o14-01 | Information Disclosure | Groq request and debug probes | high | mitigate | Record only endpoint pathname, status, sanitized provider metadata, timing, counts, and lengths; prohibit credentials, headers, bodies, transcript text, and generated text; scrub all artifacts. |
| T-260905-o14-02 | Tampering | Provider error response parsing | medium | mitigate | Treat response JSON as untrusted, allowlist only classification fields, bound parsing, and expose stable local error codes rather than raw provider bodies. |
| T-260905-o14-03 | Denial of Service | Three-format requests and 429 retry loop | medium | mitigate | Preserve bounded retries and timeout, honor bounded Retry-After values, and use runtime evidence to correct any concurrency-induced throttling. |
| T-260905-o14-04 | Spoofing | Groq authentication failures | medium | mitigate | Preserve direct HTTPS API endpoint usage and distinguish 401/403 credential rejection from model, quota, provider, and transport failures. |
</threat_model>

<verification>
1. The confirmed current failure is reproduced twice and classified with sanitized status/provider metadata at the production `summarise` boundary.
2. At least three competing hypotheses are tested; the selected root cause has toggle-and-revert proof.
3. The new regression fails before the fix and passes afterward; the complete Node suite and production build pass after implementation and cleanup.
4. Real extension QA exercises clicking Summarise and viewing all three formats, or the executor records the exact technical blocker and labels the closest runtime evidence partial.
5. Final diff and artifact scans show no credential, Authorization value, transcript, generated summary, raw provider body, debug output, or unrelated worktree mutation.
</verification>

<success_criteria>
- A user with a valid saved Groq key and available transcript can click Summarise and receive non-empty summary output.
- Provider/request failures are no longer mislabeled wholesale as an internet-connectivity problem.
- The key-validation and transcript-fetch fixes remain passing and behaviorally unchanged.
- The root cause and fix are protected by failing-first tests and realistic, sanitized runtime evidence.
</success_criteria>

<source_audit>
| Source | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | Clicking Summarise with a valid key and transcript yields summaries | 01 | COVERED | Tasks 1-2 cover the service-to-popup path and all formats. |
| REQ | — | No ROADMAP phase requirement IDs are assigned to this quick task | — | COVERED | Quick work is tracked outside phase requirements. |
| RESEARCH | — | No research artifact was requested | — | COVERED | Task 1 performs runtime diagnosis before selecting the fix. |
| CONTEXT | — | Preserve successful key validation and transcript retrieval | 01 | COVERED | Tasks 1-3 explicitly protect and re-run those regressions. |
</source_audit>

<output>
Create `.planning/quick/260905-o14-diagnose-and-fix-the-summarise-action-fa/260905-o14-SUMMARY.md` when done.
</output>
